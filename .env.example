# Environment example for CE Empire server

DATABASE_URL=postgres://user:password@localhost:5432/ce_empire
SESSION_SECRET=change-me
SESSION_COOKIE_NAME=sid
NODE_ENV=development
PORT=3000

# For seeding the initial demo user run the seed script with SEED_PASSWORD set:
# SEED_PASSWORD=YourStrongPass123 node dist/seed-user.js
